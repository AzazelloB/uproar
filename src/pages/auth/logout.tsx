const LogoutPage = () => {
  // const { setToken } = useAuthContext();
  // setToken(null);

  // const prevURL = usePrevURL();
  // const redirectURL = `${document.location.origin}${encodeURIComponent(prevURL)}`;
  // const url = `${AUTH_LOGOUT_URI}?next=${redirectURL}`;

  // window.location.assign(url);

  return null;
};

export default LogoutPage;
